{
  "name": "@code/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap":"lerna bootstrap",
    "start": "lerna exec --scope @code/app -- yarn start"
  },
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^6.5.1"
  }
}
